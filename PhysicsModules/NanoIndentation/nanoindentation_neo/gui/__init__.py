# Legacy Nano Neo GUI (tkinter), copied as-is from the original nano_indent
# repo. These scripts import each other by sibling-module name and are meant
# to be run directly (python nano_neo_gui.py) from this directory; they are
# not yet wired into the package entry points.
